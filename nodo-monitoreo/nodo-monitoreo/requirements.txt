# Dependencias de Python del firmware del nodo.
# Se instalan con:  pip install -r requirements.txt
smbus2==0.5.0          # comunicacion I2C con el sensor AHT10
paho-mqtt==1.6.1       # cliente MQTT para transmitir las mediciones
